# Core dependencies
torch>=2.0.0
transformers>=4.36.0
accelerate>=0.25.0
datasets>=2.14.0
sentence-transformers>=2.2.0
numpy>=1.24.0
scikit-learn>=1.3.0
tqdm>=4.65.0
pyyaml>=6.0
wandb>=0.15.0
faiss-cpu>=1.7.4
mteb>=1.12.0
scipy>=1.10.0

# Optimization (optional but recommended)
# flash-attn>=2.0.0  # Uncomment for Flash Attention (requires CUDA 11.6+)
# xformers>=0.0.20   # Uncomment for xFormers
onnx>=1.14.0
onnxruntime>=1.16.0
# onnxruntime-gpu>=1.16.0  # Uncomment for GPU inference
# tensorrt>=8.6.0     # Uncomment for TensorRT (NVIDIA GPUs only)

# Multilingual support
# transformers[sentencepiece]  # For some translation models
